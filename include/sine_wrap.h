#ifndef SINE_WRAP_H
#define SINE_WRAP_H

/*
#include <sq_derivable.h>
#include <opencv2/opencv.hpp>
#include <vector>
#include <renderer.h>
#include <misc_math.h>

class SineWrap : public SQDerivable {
	public:
		SineWrap(cv::Point2f beginning, float amplitude, float period, size_t repeats);
		bool advance(float index); //Advance function_index by amount, returns true if successful
		bool is_at_beginning(); 
	private:
		float amplitude;
		float period;
		size_t repeats;
		cv::Point2f beginning;
};
*/

#endif
